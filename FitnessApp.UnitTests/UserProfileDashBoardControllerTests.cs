using Xunit;
using Microsoft.AspNetCore.Mvc;
using WorkoutTrackingApp.Controllers;
using WorkoutTrackingApp.ViewModels;

namespace WorkoutTrackingApp.Tests
{
    public class UserProfileDashboardControllerTests
    {
        [Fact]
        public void Index_ReturnsCorrectViewModel()
        {
            // Arrange
           // var controller = new UserProfileDashboardController();

            // Act
           // var result = controller.Index();

            // Assert
            //var viewResult = Assert.IsType<ViewResult>(result);
            //var model = Assert.IsType<UserDashboardViewModel>(viewResult.Model);
            
           // Assert.NotNull(model.RecentWorkouts);
           // Assert.NotNull(model.Messages);
        }
    }
}
